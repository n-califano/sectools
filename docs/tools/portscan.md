# portscan.py

It provides a generic sweep of all tcp ports to detect open ports, followed by a more thorough service detection scan on the detected open ports.

```bash
python portscan.py --target <TARGET_IP>
```